# App
NODE_ENV=development
PORT=3000

# Database (Render)
# Find these in: Render Dashboard → your PostgreSQL service → Connections → External Database URL
DB_HOST=dpg-<id>.region-postgres.render.com
DB_PORT=5432
DB_USER=your-db-user
DB_PASS=your-db-password
DB_NAME=your-db-name
DB_SSL=true
